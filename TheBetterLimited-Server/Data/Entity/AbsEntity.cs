using System;
namespace TheBetterLimited_Server.Data.Entity
{
	public class AbsEntity
	{
		//public Object Copy<T>(Object o)
  //      {
  //          T? obj2 = new T();
  //          var propInfo = o.GetType().GetProperties();
  //          foreach (var item in propInfo)
  //          {
  //              obj2.GetType().GetProperty(item.Name).SetValue(obj2, item.GetValue(o, null), null);
  //          }
  //          return obj2;
  //      }
	}
}

